
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // This comment is added to trigger a re-deployment of Firestore indexes.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
